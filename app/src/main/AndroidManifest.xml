<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.te_leasetracker_c868">
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/appTitle"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning"
        android:allowBackup="false"
        android:requestLegacyExternalStorage="true">

        <activity android:name=".CarAddEditActivity"></activity>
<!--    Car Edit Activity   -->
<!--    Car Detail Activity  -->
        <activity
            android:name=".CarDetailActivity">
<!--            <meta-data-->
<!--                android:name="PARENT_ACTIVITY"-->
<!--                android:value="com.example.te_leasetracker_c868.CarListActivity" />-->
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CarListActivity"
            android:label="@string/title_activity_car_list" />
    </application>

</manifest>